/*Create a function that displays a pattern like this:
      1 
    1
  1 
1
*/


function obliquelineRight(row) {
    for (let i = size; i >= 0; i--);

    var row = " ";
    for (let j = 0; j < size; j++);
    if (i === j ) {
        row += "1";
    }
    else {
        row += " ";
    }
    console.log(row);
}
