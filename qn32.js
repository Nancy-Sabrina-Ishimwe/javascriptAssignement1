/*Create a function that receives two numbers and returns the bigger number.*/

function maxNumber(num1, num2) {
    return Math.max(num1, num2);
   }
   
   console.log(maxNumber(5, 10)); // Outputs: 10
   