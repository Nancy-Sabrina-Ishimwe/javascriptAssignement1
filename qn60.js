//Write a function that calculates the perimeter of a rectangle.

function calculateRectanglePerimeter(length, width) {
    return 2 * (length + width);
   }
   
   console.log(calculateRectanglePerimeter(5, 6)); // Outputs: 22
   